# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated by `npm run verify:seo` on every run. Kept out of git so the
# working tree stays clean between audits.
reports/seo-verification.*

# Stale FUSE editor temp copies
.fuse_hidden*

# SERVER-SIDE SECRETS. server/.env holds the SMTP credentials (Gmail App
# Password) and must NEVER be committed. This is the opposite of the root
# .env, which is tracked on purpose because VITE_ values are public anyway.
server/.env
server/node_modules
