// The site root. A static Route String outranks the catch-all Route Function in
// pages/index/+route.ts, and that catch-all now explicitly declines "/" too, so
// exactly one page ever matches the root URL.
export default "/";
